{
  "name": "luxury-access-protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Backend reconstruction for a regulated luxury asset access marketplace. Not production-ready.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "demo": "npx tsx src/demo/scenarios.ts"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/pg": "^8.18.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.20.0"
  }
}
